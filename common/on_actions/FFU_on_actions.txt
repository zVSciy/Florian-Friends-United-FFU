# on_action = {
	

# on_startup = {
# 	# Add 15 Army/Navy/Air Force experience
# 	effect = {
# 		every_country = {
# 			army_experience = 15
# 			navy_experience = 15
# 			air_experience = 15
# 		}
# 	}

# }

# }

on_actions = {

    on_startup = {
		effect = {
			every_country = {
				set_rule = {
					can_send_volunteers = no
				}
			}
			if = {
				limit = {
					has_game_rule = {
						rule = china_war_rule
						option = ENABLED
					}
				}
				every_country = {
					limit = {
						OR = {
							tag = SOV
							tag = ENG
							tag = USA
							tag = FRA
							tag = AST
							tag = CAN
							tag = SAF
							tag = RAJ
                            tag = BRA

							tag = GER
							tag = ITA
                            tag = SPR
							tag = HUN
							tag = ROM
							tag = BUL
							tag = YUG
						}
					}
					set_rule = {
						can_send_volunteers = yes
					}
                    add_ideas = volunteer_minimum_minor
				}
			}
		}
	}

	on_capitulation = {
		effect = {
			if = {
				limit = {
					original_tag = CHI
				}
				every_country = {
					limit = {
						OR = {
							tag = SOV
							tag = ENG
							tag = USA
							tag = FRA
							tag = AST
							tag = CAN
							tag = SAF
							tag = RAJ
                            tag = BRA

							tag = GER
							tag = ITA
                            tag = SPR
							tag = HUN
							tag = ROM
							tag = BUL
							tag = YUG
						}
					}
					delete_unit_template_and_units = { division_template = "China War Vol" }
				}
			}
		}
	}
}